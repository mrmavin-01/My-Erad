name: Build KivyMD APK

on:
  push:
    branchesjobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: 3.9

    - name: Install dependencies
      run: |
        pip install --upgrade pip
        pip install buildozer
        pip install cython

    - name: Set up Android SDK
      run: |
        mkdir -p $HOME/.buildozer/android/platform
        cd $HOME/.buildozer/android/platform
        wget https://dl.google.com/android/repository/commandlinetools-linux-8092744_latest.zip
        mkdir cmdline-tools
        unzip commandlinetools-linux-8092744_latest.zip -d cmdline-tools
        rm commandlinetools-linux-8092744_latest.zip
        export PATH=$PATH:$HOME/.buildozer/android/platform/cmdline-tools/bin
        yes | $HOME/.buildozer/android/platform/cmdline-tools/bin/sdkmanager --sdk_root=$HOME/.buildozer/android/platform cmdline-tools latest
        yes | $HOME/.buildozer/android/platform/cmdline-tools/bin/sdkmanager --licenses
        yes | $HOME/.buildozer/android/platform/cmdline-tools/bin/sdkmanager "build-tools;36.0.0-rc3"
        yes | $HOME/.buildozer/android/platform/cmdline-tools/bin/sdkmanager "extras;google;m2repository"

    - name: Build the APK
      run: |
        buildozer android debug
